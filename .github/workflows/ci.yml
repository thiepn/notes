name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: PWA offline certification
        run: |
          set +e
          npm run e2e:pwa 2>&1 | tee pwa-output.txt
          status=${PIPESTATUS[0]}
          exit $status

      - name: Upload PWA diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: v2-1-pwa-diagnostics
          path: |
            pwa-output.txt
            playwright-report
            test-results
          if-no-files-found: ignore
